# LANE M1 — Server patch layer: vector-store selector, health endpoints, boot probe, provisioning script, tests
[wave 1 · depends: M0 MERGED · taxonomy PATCH · model Opus 4.8 · **P6.5: YES (auth-adjacent) — post plan, Lucas ack before coding**]

## 1. Objective
Implement the entire server-side LetA patch (the only Python-code delta over upstream):
env-driven vector-store selection with a Qdrant builder that survives the QdrantConfig
validator, `/healthz`+`/readyz`, request-log skip, fail-closed boot probe, and the delta-6
member-credential provisioning script — with regression tests T-1..T-15. Realizes spec
03-M §4.1-4.2, §5.1-5.3, §3.4 delta-6, §9.4.

## 2. Scope / Non-scope
In: `server/main.py` (additive only), `server/scripts/provision_service_key.py`,
`tests/server/test_leta_qdrant_config.py`. Out: Dockerfile/deploy (M2), CI (M3), any
`mem0/**` core change (never), any auth.py / routers change (**wall** — never weaken admin
gating; the provisioning script uses existing helpers, it does not modify them). Do NOT
re-add what upstream fixed: /search wrapper, OPENAI_BASE_URL forwarding (spec §3.1-3.2 —
re-verify at P4, then leave alone).

## 3. Repos and branch
miman, `main` (M0 merged) — if `server/main.py` or `mem0/configs/vector_stores/qdrant.py`
changed since `cd79fa89`: PARK (spec citations must be re-grounded first). Worktree
`../wt-m1`, branch `lane/m1-server-patch`.

## 4. Read first (closed set)
Spec 03-M §3 (all), §4.1-4.2, §5.1-5.3, §7.4, §9.3-9.4, §13 R-3/R-6 · tracker (gate, DV-1,
B-4 note) · bridge (B-4 status — if Lucas NACKED the delta-6 flow, PARK the provisioning
part only, deliver the rest, report split) · upstream code cited: server/main.py:58,
107-142, 261-267, 321-323; mem0/configs/vector_stores/qdrant.py:11-38;
server/auth.py:15-20,105-141,144-190,193-214; server/routers/api_keys.py:62-76;
server/models.py:25; server/scripts/reset_admin_password.py (pattern);
server/db.py (SessionLocal).

## 5. Inspect
main.py:119-142 — confirm DEFAULT_CONFIG shape + `initialize_state` call site; decide
insertion point for selector (module-level, before line 119's dict). · qdrant.py:31-38 —
confirm validator branches (url+api_key / host+port / path). · auth.py `generate_api_key`
+ `hash` helpers — exact signatures for the script. · Existing upstream tests under
`tests/` that import server/main (avoid double-init side effects; check how/if upstream
tests spin the app).

## 6. Requirement checklist (each maps to a §9.4 test)
R1 selector contract spec §5.1 (unset→pgvector byte-identical; qdrant; fail-fast else;
   trim+lowercase) → T-1,2,7. R2 Qdrant builder spec §5.2 exactly (url+key pass-through;
   url-no-key → host/port decomposition + https flag; host/port fallback; dims/on_disk
   coercions; never `path`) → T-3,4,5,6. R3 `/healthz` `/readyz` spec §5.3 semantics →
   T-8,9. R4 both paths + log-skip additions → T-12. R5 boot probe: startup hook,
   `MIMAN_ENV != local` + qdrant ⇒ one `get_collections()` round-trip, exit non-zero on
   failure; local ⇒ warn → T-15. R6 provisioning script: args `--name --label`
   [--rotate]; creates `role="member"` user + APIKey via upstream helpers; prints full key
   ONCE to stdout; idempotent-by-name (existing user ⇒ mint new key only with --rotate,
   else refuse); never logs the key → new tests T-16 (member user created, role member),
   T-17 (key resolves via `_resolve_user_from_api_key` to member; simulate require_admin
   → 403 expectation at unit level). R7 OPENAI_BASE_URL env honored with NO server code
   (core path) → T-10. R8 /search both shapes → T-11. R9 infer=false single-record
   (embedder mocked) → T-13. R10 no MEM0_API_KEY in canonical config → T-14.

## 7. Call-graph / data-flow map
Trace and quote in-report: env → selector → `DEFAULT_CONFIG["vector_store"]` →
`initialize_state` (server_state.py) → `get_memory_instance()` → `VectorStoreFactory`
(mem0/utils/factory.py:180) → `QdrantConfig` validation → `Qdrant.__init__` client params.
Discrepancy vs spec §5.2 assumptions ⇒ PARK.

## 8. Implementation plan (post at P6.5, wait for Lucas ack)
Tests first where practical: (1) port/rewrite test file with T-1..T-17 red → (2) selector
+ builders + env block → (3) health endpoints + skip paths → (4) startup probe + MIMAN_ENV
→ (5) provisioning script → (6) green + lint. Keep every main.py addition in ONE
contiguous `# --- LetA patch (see LETA_PATCH.md) ---` region where feasible (merge-friction
control, spec R-4).

## 9. Acceptance criteria
A1-A17 = T-1..T-17 each green: `python -m pytest tests/server/test_leta_qdrant_config.py -q`.
A18 upstream suite untouched-and-green for the SDK subset the gate recorded at M0.
A19 `git diff main --name-only` == §12 list. A20 grep-proof: no edits inside any existing
upstream function in main.py (`git diff -U0 main -- server/main.py` shows additions only,
plus the one-line SKIPPED_REQUEST_LOG_PATHS constant change — justify any other hunk).

## 10. Test cases
`tests/server/test_leta_qdrant_config.py` — T-1..T-15 per spec §9.4 table + T-16/T-17
(R6). Fixtures: env-patching helper (mirror deprecated `_option_b_env` pattern,
READ-ONLY reference deprecated tests:71-88); fake qdrant client for T-15; embedder mock
for T-13. Negative cases are merge-blocking: T-4 (validator quirk), T-7, T-15-prod-fail.

## 11. Validation commands
Gate from tracker + `python -m pytest tests/server/ -q` +
`python -m ruff check server/main.py server/scripts/provision_service_key.py tests/server/`
+ A20 diff commands.

## 12. Expected files changed
`server/main.py` (additive) · `server/scripts/provision_service_key.py` (new) ·
`tests/server/test_leta_qdrant_config.py` (new) · `LETA_PATCH.md` (patch table rows) ·
tracker.

## 13. Risks, blast radius, rollback
Blast radius: server boot path (selector runs at import). Top risks: import-time env reads
break upstream tests that import main (mitigate: selector defaults preserve upstream when
unset — T-1 proves); QdrantConfig validator drift (T-4 pins); double-init in tests
(inspect §5 first). Rollback: revert PR — zero data writes.

## 14. Report format
Template terse report + explicit statement of B-4 bridge status found at P0 + the P6.5
plan-ack link/quote. Contract note: /healthz + /readyz are contract ADDITIONS already
disclosed via bridge (B-3) — no new bridge entry needed unless shapes deviate from spec.
