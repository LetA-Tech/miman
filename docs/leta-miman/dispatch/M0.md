# LANE M0 — Repo bootstrap: upstream remote, LETA_PATCH.md v1, CI-gate reality check
[wave 1 · depends: none · taxonomy INFRA · model Sonnet 5 · P6.5: no]

## 1. Objective
Make `LetA-Tech/miman` operable as the LetA runtime repo: upstream remote wired for future
syncs, the patch manifest (`LETA_PATCH.md`) created with the base pin, and the CI gate
commands proven against repo reality and recorded in the tracker. Realizes spec 03-M §1.2,
§11 M0.

## 2. Scope / Non-scope
In: git remote; `LETA_PATCH.md` (repo root); tracker CI-gate section finalized; local
test-run bootstrap documented. Out: ALL server/docker/CI code (M1-M3); branch-protection
GitHub settings (Lucas gate — record as follow-up, do not attempt via API). Do-not-touch:
every upstream file; `.github/**`; `docs/` Mintlify tree.

## 3. Repos and branch
`/Users/lucas/Projects/miman`, base `main` @ `cd79fa89` (drift ⇒ re-verify spec pins, note
in report — not an automatic PARK for M0 unless server/mem0 files changed). Worktree
`../wt-m0`, branch `lane/m0-bootstrap`.

## 4. Read first (closed set)
Spec 03-M: ground-truth pins, naming register, §1.2-1.3, §4.6, §6.4 (LETA_PATCH base rules),
§12 item 1 · tracker (CI-gate proposal) · deprecated `~/leta/mem0-v2.0-deprecated/
LETA_PATCH.md` (READ-ONLY format reference).

## 5. Inspect
`git remote -v` (expect origin only) · `git log --oneline -3` vs pin · `pyproject.toml`
version · confirm `pytest`/`ruff` availability path (hatch env vs venv vs system pip) —
answer: what exact commands make `pytest tests/` runnable on this machine.

## 6. Requirement checklist
R1 upstream remote `https://github.com/mem0ai/mem0.git` added (verify: `git remote -v`).
R2 `LETA_PATCH.md` exists at repo root: base pin (`cd79fa89`, package 2.0.11, "re-pin to
   next upstream v-tag at first MS lane"), the §4.6 KEEP/DROP table, tag rule
   (`miman-vX.Y.Z` only, bare `v*` forbidden — spec §6.3), pointer to
   `docs/leta-miman/03-M-MeM0-detailed-spec.md` as design authority.
R3 Tracker CI-gate section replaced with VERIFIED commands incl. env bootstrap (e.g.
   `pip install -e . && pip install -r server/requirements.txt` in a venv — whatever
   actually works, proven by running upstream's existing test subset once).
R4 `git diff cd79fa89..HEAD --name-only` = exactly the files this lane adds (proves clean
   base; verify command in report).

## 7. Call-graph / data-flow map
n/a — no code paths touched (justified: docs + git config only).

## 8. Implementation plan
(1) remote add + fetch upstream --tags (record newest upstream `v*` tag in LETA_PATCH.md
as "sync horizon") → (2) write LETA_PATCH.md → (3) prove CI gate commands, record in
tracker → (4) tracker close-out.

## 9. Acceptance criteria
A1 `git remote -v` shows origin + upstream — PASS/FAIL.
A2 `test -f LETA_PATCH.md && grep -q cd79fa89 LETA_PATCH.md` — PASS/FAIL.
A3 Recorded gate commands run green end-to-end from a fresh shell (paste output).
A4 R4 diff check — PASS/FAIL.

## 10. Test cases
n/a beyond A3 (justified: no behavior changed; A3 proves the harness itself).

## 11. Validation commands
`git remote -v` · `git fetch upstream --tags --dry-run` · gate commands from R3 ·
`git diff cd79fa89..HEAD --name-only`.

## 12. Expected files changed
`LETA_PATCH.md` (new) · `docs/leta-miman/dispatch/M_LANE_TRACKER.md` (CI gate + status).
Nothing else.

## 13. Risks, blast radius, rollback
Near-zero blast radius (additive docs). Risk: wrong env-bootstrap recipe poisons every
later lane — mitigate by proving commands in a clean venv. Rollback: revert PR.

## 14. Report format
Per template: entry checks, R1-R4 PASS/FAIL with command output, files changed, 3-line
summary. Follow-ups to tracker: "branch protection on main (Lucas, GitHub settings)".
